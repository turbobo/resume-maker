# 简履 Resume Maker — 技术方案文档

> 最后更新：2026-09-15（接入商汤 AI 行业分析 + 服务端 IP 限流 + AI 分析准入门槛）

## 一、总体架构

纯前端 SPA（浏览器本地编辑简历）+ EdgeOne Cloud Functions 服务端（AI 代理与限流）。API Key 只保存在服务端环境变量，前端不接触。

```
┌──────────────┐   POST /api/ai/*   ┌─────────────────────────┐  Bearer Key   ┌────────────────┐
│ 前端 SPA      │ ─────────────────► │ EdgeOne Cloud Functions │ ────────────► │ 商汤日日新 API   │
│ Vite + React │ ◄───────────────── │ 限流 → 校验 → 拼 Prompt 转发 │ ◄──────────── │ OpenAI 兼容协议  │
└──────────────┘   JSON 响应         └─────────────────────────┘               └────────────────┘
```

- 前端技术栈：Vite 8 + React 19 + TypeScript + Tailwind CSS v4 + Zustand（localStorage 持久化）
- 服务端：EdgeOne Cloud Functions（Node.js 22，文件系统路由），无数据库
- AI 服务：商汤日日新 SenseNova，OpenAI 兼容协议，默认端点 `https://token.sensenova.cn/v1`，默认模型 `deepseek-v4-flash`
- 商汤网关约束：请求参数表未列出的字段会被拒收（如 `response_format`），请求体保持最小集 `model / messages / temperature / max_tokens`；`temperature ∈ [0,2]`、`max_tokens ∈ [1, 65536]`

### 关键约束

- 函数单次运行时长上限 120s；项目经 `edgeone.json` 配置 `maxDuration: 60`，上游请求超时 55s
- Cloud Functions 为多实例 Serverless，限流基于实例内存（见 §四）
- 静态资源与 `/api/*` 同域部署，无跨域问题；前端使用相对路径请求

## 二、服务端接口（cloud-functions）

### 目录结构

```
cloud-functions/api/ai/
├── analyze.js             # POST /api/ai/analyze 行业分析
├── rewrite.js             # POST /api/ai/rewrite 内容优化
└── _shared/
    ├── http.js            # 统一响应工具（jsonResponse / errorResponse / pickText）
    ├── sensenova.js       # 商汤 API 调用封装（超时、错误码映射、JSON 提取）
    ├── rateLimit.js       # 滑动窗口限流器（纯逻辑）+ 429 响应构造
    └── aiLimiter.js       # 共享限流器实例 + 客户端 IP 解析
```

下划线前缀目录不生成路由（构建器仅将导出 handler 的文件识别为函数）。

### 统一响应格式

- 成功：`{ success: true, data: {...} }`
- 失败：`{ success: false, code: string, message: string }`（429 时附 `retryAfter` 秒数 + `Retry-After` 头）

### POST /api/ai/analyze — 行业分析

请求：

```json
{ "industry": "互联网 / IT", "data": { "name": "", "title": "", "summary": "", "skills": "", "experiences": [], "projects": [], "education": [], "customSections": [] } }
```

响应 `data`：

```json
{
  "score": 0-100, "industryFit": 0-100, "verdict": "一句话总评",
  "highlights": ["亮点"],
  "issues": [{ "severity": "error|warning|tip", "message": "问题", "suggestion": "建议" }],
  "suggestions": [{ "title": "建议标题", "detail": "具体改法" }],
  "keywords": ["行业关键词"]
}
```

- 行业白名单 12 项，定义于 `analyze.js`（与前端 `src/utils/resumeAnalyze.ts` 的 `INDUSTRY_OPTIONS` 保持一致，修改需同步两处）
- 简历序列化为紧凑文本后送模型，总长截断 8000 字符、单字段截断 400 字符
- 模型输出经 `extractJson`（兼容代码块包裹）解析，再归一化：评分 clamp 0-100、数组过滤非字符串/超长项

### POST /api/ai/rewrite — 内容优化

请求：`{ "action": "<枚举>", "payload": {...} }`，响应 `{ success: true, data: { text } }`。

| action | 用途 |
|--------|------|
| polishExperience | 润色工作经历描述 |
| generateSummary | 生成个人简介 |
| polishProject | 润色项目经历 |
| suggestSkills | 推荐技能关键词 |
| polishCustomSection | 润色自定义模块 |

Prompt 模板固定在服务端（action 白名单），前端只传结构化数据，防止被当作任意 LLM 网关滥用。

### 错误码

| code | HTTP | 说明 |
|------|------|------|
| AI_NOT_CONFIGURED | 503 | 服务端未配置 SENSENOVA_API_KEY |
| INVALID_BODY / INVALID_INDUSTRY / INVALID_RESUME / EMPTY_RESUME | 400 | 请求校验失败 |
| RATE_LIMITED | 429 | 触发本服务限流（带 retryAfter） |
| UPSTREAM_RATE_LIMITED | 429 | 商汤侧限流 |
| UPSTREAM_AUTH_ERROR | 502 | 商汤 API Key 无效 |
| UPSTREAM_TIMEOUT | 504 | 上游超时（55s） |
| UPSTREAM_EMPTY / AI_PARSE_ERROR | 502 | 模型输出为空 / 非合法 JSON |
| INTERNAL_ERROR | 500 | 其他异常 |

## 三、环境变量（EdgeOne Pages 控制台配置）

> API Key 统一由控制台环境变量提供，不硬编码进代码（本仓库为公开仓库，硬编码会导致 Key 泄露）。

| 变量 | 必填 | 默认 | 说明 |
|------|------|------|------|
| SENSENOVA_API_KEY | 是 | - | 商汤 API Key（未配置时接口返回 503） |
| SENSENOVA_BASE_URL | 否 | `https://token.sensenova.cn/v1` | OpenAI 兼容端点（旧版兼容模式可改为 `https://api.sensenova.cn/compatible-mode/v1`） |
| SENSENOVA_MODEL | 否 | `deepseek-v4-flash` | 模型 ID（免费档 150 次/5 小时；可用 `sensenova-6.7-flash-lite` 等） |
| AI_RATE_WINDOW_MAX | 否 | 8 | 单 IP 短窗口（10 分钟）上限 |
| AI_RATE_DAILY_MAX | 否 | 50 | 单 IP 每日上限 |
| AI_RATE_GLOBAL_DAILY_MAX | 否 | 600 | 全站每日总上限 |

## 四、限流策略

滑动窗口，三个维度同时生效（任一触发即返回 429）：

| 维度 | 默认阈值 | 目的 |
|------|---------|------|
| 单 IP 短窗口 | 10 分钟 8 次 | 防连续刷接口 |
| 单 IP 每日 | 50 次 | 防单用户滥用 |
| 全站每日 | 600 次 | 保护商汤账号额度（deepseek-v4-flash 免费档 150 次 / 5 小时，滚动窗口一天最多约 720 次，取 600 留余量） |

- 实现：`createRateLimiter`（内存 Map），北京时间 0 点重置每日计数；`aiLimiter.js` 惰性初始化并让 analyze / rewrite 共用同一份额度
- 客户端 IP 来源：仅使用 `context.clientIp`（平台注入，客户端无法伪造）；不读 `x-forwarded-for` 等请求头，避免伪造头绕过限流；极端情况 clientIp 缺失时共享 `unknown` 额度
- 局限：Serverless 多实例内存独立，实际阈值会被实例数放大；实例回收后计数清零。作为防刷第一道防线，精确分布式限流需引入 EdgeOne KV（后续可选增强）
- 响应携带 `Retry-After` 头与 `retryAfter` 字段，前端据此提示用户

## 五、前端模块

| 文件 | 职责 |
|------|------|
| `src/utils/aiClient.ts` | AI 接口统一客户端：`postAI` 封装 fetch、错误归一化为 `AIRequestError`（code / message / retryAfter） |
| `src/utils/aiAccess.ts` | AI 分析准入门槛：体检分 ≥ 90 且核心信息完整（姓名 / 邮箱 / 手机号 / 简介 40 字 / 首段经历 / 首段教育 / 技能 6 个）才允许分析；返回 `{ allowed, score, blockers }` 供面板展示待完成项 |
| `src/utils/resumeAnalyze.ts` | 行业选项常量、`AnalyzeResult` 类型、`analyzeResume()`；请求前剔除照片/字体等非内容字段 |
| `src/utils/aiGenerator.ts` | 5 个内容优化函数（改造）：全部走 `/api/ai/rewrite`，不再读写 localStorage |
| `src/components/AIAnalysisPanel.tsx` | AI 行业分析面板：行业标签选择、分析触发、loading / 错误 / 结果展示；分析前校验准入门槛（未达标展示进度与待完成项并禁用按钮）；组件卸载时 abort 未完成请求 |

交互约定：切换行业清空上一次结果；分析结果不持久化；错误提示直接展示服务端 message（含限流剩余时间文案）。

## 六、构建与部署

- `edgeone.json`：`buildCommand: npm run build`、`installCommand: npm install && npm install --no-save @rolldown/binding-linux-x64-gnu@1.1.4`（补装 rolldown 的 Linux 原生绑定，版本需与 `rolldown` 同步升级）、`outputDirectory: dist`、`nodeVersion: 22.21.1`（Vite 8 要求 20.19+/22.12+）、`cloudFunctions.nodejs.maxDuration: 60`
- 部署流程：推送到 GitHub → EdgeOne 控制台手动触发部署（禁用自动构建）
- 环境变量在控制台「项目设置 → 环境变量」配置后需重新部署生效
- 本地开发：
  - 纯前端：`npm run dev`（AI 接口返回 404，面板显示"AI 服务不可用"属预期）
  - 全栈联调：另开终端 `edgeone pages dev --fePort=5173`（需 `npm i -g edgeone`，支持读取控制台同步的环境变量）

## 七、测试

`npm test`（Node 内置 test runner，零依赖）：`tests/ai-functions.test.mjs` 共 15 个用例，覆盖

- 限流器：短窗口拦截与恢复、每日上限、全站上限、多 IP 隔离
- `extractJson`：纯 JSON / 代码块包裹 / 前后带文字 / 非法输入
- analyze handler：未配置 Key 503、无效行业 400、非法 body 400、正常流程字段归一化、非法模型输出 502、超限 429（带 Retry-After）
- rewrite handler：无效 action 400、正常返回、上游 401/429 错误映射

## 八、安全设计要点

- API Key 仅存服务端环境变量，日志与响应均不回显
- 所有外部输入校验：body 必须为 JSON 对象、行业白名单、字段类型与长度截断
- 上游错误映射为内部错误码，不透传商汤原始响应体（截断 200 字符）
- 照片 base64 不参与分析请求，降低隐私与带宽风险

## 九、导出功能实现（Word / PDF）

### Word 导出（src/utils/exportDocx.ts）

按模板类型映射版式（与预览结构对齐）：

| 模板 | 版式 | 侧栏内容 | 简介位置 |
|------|------|---------|---------|
| modern / executive | 双栏（表格） | 照片+姓名+联系方式+技能+教育（固定 技能→教育） | 主栏顶部 |
| minimal | 双栏（表格，无底色） | 跟随 sectionOrder 的侧栏模块 | 跟随 sectionOrder |
| classic / compact | 单栏 | — | 跟随 sectionOrder |

关键实现：

- 双栏用单行两列表格（2600/6600 DXA，TableLayoutType.FIXED，modern/executive 侧栏 f5f5f4 底色）
- 章节标题统一走 `getSectionLabel`，支持用户自定义模块名
- 多行文本按行拆为独立段落（对齐预览 whitespace-pre-line 效果）
- 侧栏标题（16 半磅）与主栏标题（22 半磅）样式分级；照片：classic 居中 / compact 居右 / 双栏置于侧栏顶部
- Blob URL 延迟 1s 释放，避免下载未开始即被回收

### Markdown 导出（src/utils/exportMarkdown.ts）

- 纯函数 `resumeToMarkdown(data)` 生成文本：`#` 姓名 / `**职位**` / 联系方式以 ` | ` 连接；章节按 sectionOrder 输出 `##` 标题；经历/项目/教育为 `### 条目` + 时间行 + `-` 要点列表；技能逐条列出
- 章节标题走 `getSectionLabel`（自定义模块名生效）；多行描述统一转为 `- ` 列表项（自动去除行首已有符号）；照片不参与（纯文本格式）
- 下载复用公共工具 `src/utils/download.ts` 的 `downloadBlob`（exportDocx 亦共用）

### PDF 导出（src/utils/exportPdf.ts）

html2canvas 截图 `.print-area` + jsPDF 分页：

- 导出前把预览缩放还原为 1，onclone 中去除 zoom/transform/box-shadow
- 输出 JPEG(0.95)：同清晰度下体积远小于 PNG（实测约 10MB → 1–2MB 量级）
- 分段超时保护：布局等待 1s、字体等待 5s、渲染 30s，任一环节挂起都会失败并提示，不会无限卡在“导出中”
- 移动端：编辑页点击导出 PDF 自动切换到预览页；`waitForPrintArea` 会等待预览内容（h1）就绪后再截图，避免懒加载未完成时截到空白页
- 联系方式图标对齐：html2canvas 对行内 SVG 的垂直对齐处理存在缺陷（图标整体偏上/大位移裁剪）；预览端使用 `translateY(-0.1em)`（浏览器墨迹级实测偏差 ≈ 0），导出端在 onclone 中把图标覆盖为 `translateY(1.6px)`（补偿值经像素级实测校准，紧凑/专业模板偏差 ≤0.4 CSS px），预览与导出解耦、两端均精确对齐

### 导出链路

TopBar（桌面）/ MobileNav 抽屉（移动）→ `useResumeActions` → 动态 import 导出模块 → 失败时 console.error + alert 提示
