# 简履 Resume Maker — 产品设计文档

> 最后更新：2026-09-15（新增 AI 行业分析功能及准入门槛）

## 一、产品定位

浏览器端零登录简历制作工具：本地编辑、实时预览、导出 Word / PDF，数据保存在浏览器本地（localStorage），不上传服务器。新增 AI 能力由服务端代理调用大模型（商汤日日新），API Key 不暴露给用户。

## 二、功能清单

### 已完成

- 简历编辑：基本信息 / 个人简介 / 技能标签 / 工作经历 / 教育背景 / 项目经历 / 自定义模块
- 模块管理：拖拽排序、重命名、增删模块
- 模板与排版：经典 / 极简 / 现代 / 紧凑 / 专业 5 套模板，标题与正文字体切换
- 桌面端布局体验：编辑面板默认 420px（可拖拽 320–560px，宽度记忆）；输入框放大（字号 14px / 更大行高）；「专注编辑」（收起预览）与「专注预览」（全屏查看，Esc 退出）一键切换
- 列表输入：多行输入框（工作内容/项目描述/简介/自定义模块等）回车自动延续有序（1. 2. / 1、2、/ 1) 2)）与无序（- • * ·）列表，空项回车退出列表；导出时保留列表标记
- 照片上传（本地压缩）、Word 导入（.docx）
- Word 导出：按所选模板产出版式 —— 现代/专业/极简输出双栏（侧栏 + 主内容）；紧凑模板照片居右；支持自定义模块名与多行要点分行
- Markdown 导出：导出 `.md` 纯文本（章节顺序与自定义模块名与其它导出一致；多行要点转为列表；不含照片）
- PDF 导出：A4 分页输出，自动还原预览缩放、去阴影；移动端在编辑页点击导出自动切换到预览并等待内容就绪（防白屏）；联系方式图标与文字精确对齐；渲染超时保护；文件体积优化
- 简历体检（ATS）：本地规则引擎评分 + 问题清单（不消耗 AI 额度）
- **AI 行业分析（本次新增）**：按目标行业由大模型给出评分、问题诊断与优化建议；设置准入门槛（体检 90 分 + 核心信息完整）以保证分析输入质量
- AI 内容优化服务端能力（润色经历 / 生成简介 / 润色项目 / 推荐技能 / 润色自定义模块），UI 入口待接入

### 待办

- [ ] 将 AI 内容优化接入各编辑区（经历、项目、简介、技能旁的"AI 优化"按钮）
- [ ] AI 分析建议一键应用到对应简历字段
- [ ] 分析结果历史记录与对比
- [ ] EdgeOne KV 分布式限流（替换实例内存限流）
- [ ] 字体本地托管（当前字体依赖 Google Fonts CDN，网络不佳时影响预览与 PDF 导出的字体保真）

## 三、AI 行业分析（新功能）

### 准入门槛

AI 分析消耗有限的大模型配额，且内容残缺时分析建议与体检提示高度重复，因此设置双重门禁，全部满足才可发起分析：

1. 简历体检分 ≥ 90
2. 核心信息完整：姓名 / 有效邮箱 / 手机号 / 个人简介 20 字以上 / 首段经历含公司、职位、起止时间且描述 50 字以上 / 首段教育含学校、学历、起止时间 / 技能关键词 6 个以上

未达标时面板展示进度条（当前体检分 / 90）与待完成项清单，分析按钮置灰并显示「达到上方条件后解锁」。

### 功能描述

用户选择目标行业后，系统将简历内容发送给大模型，由"该行业资深招聘官"视角输出结构化诊断报告。

### 使用流程

1. 编辑区展开「AI 行业分析」卡片
2. 从 12 个预设行业标签中选择目标行业（互联网 / IT、人工智能 / 数据、金融 / 财务、教育培训、医疗健康、制造业、消费零售、传媒 / 广告、法律 / 合规、人力资源、咨询 / 服务、其他行业）
3. 点击「开始分析」，等待约 10-30 秒（按钮显示转圈与进度文案）
4. 查看结果：
   - **双评分**：综合评分 + 行业匹配度（0-100，颜色分级：≥80 绿 / ≥60 琥珀 / <60 红）
   - **一句话总评**
   - **亮点**：简历已有优势（绿勾列表）
   - **问题诊断**：3-6 条，按严重度分级（红=硬伤 / 黄=警告 / 蓝=提示），每条附修改建议
   - **优化建议**：3-5 条面向所选行业的具体改法（标题 + 详情）
   - **建议关键词**：该行业简历应出现的关键词标签
5. 可切换行业后重新分析，或点击「重新分析」刷新结果

### 交互约定

- 切换行业时自动清空上一次结果，避免旧结果误导
- 结果不持久化（刷新页面后需重新分析）
- 分析期间按钮禁用并显示"正在分析，约需 10-30 秒…"；未达准入门槛时按钮置灰并展示待完成项
- 出错时按钮下方显示红色提示：限流时显示剩余等待时间，服务未配置时提示服务不可用

### 使用限制（防滥用）

| 限制 | 默认值 |
|------|--------|
| 单 IP 10 分钟内 | 8 次 |
| 单 IP 每日 | 50 次 |
| 全站每日 | 600 次（按上游模型 deepseek-v4-flash 免费配额设定） |

- 超出后提示"操作过于频繁，请 X 秒后重试"或"今日使用次数已达上限，请明日再试"
- 每日额度按北京时间 0 点重置
- 隐私：分析请求不包含照片；简历内容仅用于本次分析，不落库

### 与「简历体检」的区别

| | 简历体检（ATS） | AI 行业分析 |
|---|---|---|
| 引擎 | 本地规则 | 大模型 |
| 视角 | 通用格式完整性 | 目标行业招聘偏好 |
| 速度 | 即时 | 约 10-30 秒 |
| 额度 | 无限制 | 受限流约束 |
| 定位 | 基础合规检查 | 深度个性化建议 |

## 四、设计规范

- 面板风格与「简历体检」一致：折叠卡片、圆角 12px、hover 变色、移动端与桌面端双适配
- 行业标签：flex 换行排布，选中态黑底白字
- 主操作按钮：全宽、黑底白字、按压缩放反馈（active:scale 0.98）
- 评分颜色语义：绿色（优秀）→ 琥珀（良好）→ 红色（需改进）
- 错误/加载状态文案（约 10-30 秒预期管理）
